# AWS EC2 배포용 환경변수 설정 예시
# 이 파일을 .env.production으로 복사하고 실제 값으로 수정하세요

# 데이터베이스 설정 (AWS DocumentDB 또는 MongoDB Atlas 사용)
DATABASE_URL=mongodb://username:password@your-ec2-instance:27017/app?authSource=admin

# Google OAuth 설정
GOOGLE_CLIENT_ID=your-google-client-id

# 서버 URL 설정 (EC2 인스턴스의 공개 IP 또는 도메인)
FRONTEND_URL=https://your-domain.com
API_PORT=8000

# CORS 설정을 위한 허용 도메인
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# SSL/TLS 설정 (운영환경에서는 HTTPS 사용)
USE_HTTPS=true

# 로깅 설정
LOG_LEVEL=INFO

# 시크릿 키 (JWT 토큰용 - 실제 배포시 강력한 랜덤 키 사용)
SECRET_KEY=your-super-secret-key-change-in-production
